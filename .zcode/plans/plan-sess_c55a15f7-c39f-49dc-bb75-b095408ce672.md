Install the two terminal-emulator skills found via find-skills, at user level (available in all projects):

1. **tauri-pty** — exact match for orchaterm's stack (Tauri 2 + React + xterm.js + PTY):
   ```
   npx skills add yofabr/tauri-pty@tauri-pty -g -y
   ```
2. **xterm-js** — xterm.js + React best practices:
   ```
   npx skills add ssv445/claude-skills@xterm-js -g -y
   ```

Then verify both landed in the user skills directory (`~/.agents/skills/` or `~/.zcode/skills/`) and give you a one-line summary of what each covers.

Note: both skills have very low install counts (2 and 11), so treat their guidance as a starting point to skim, not authoritative — I'll flag anything that conflicts with orchaterm's existing PTY backend if we use them later.